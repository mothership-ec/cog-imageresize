<img src="{{ url }}" {% if width %}width="{{ width }}"{% endif %} {% if height %}height="{{ height }}"{% endif %} alt="{{ altText }}" {% for attr, val in attributes %} {{attr}}="{{ val }}" {% endfor %}>